<Window x:Class="Solitaire.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Solitaire" Height="450" Width="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition/>
      <RowDefinition/>
    </Grid.RowDefinitions>
    <ListView ItemsSource="{Binding Waste}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextBlock>
            <TextBlock.Text>
              <MultiBinding StringFormat=" {0} - {1}">
                <Binding Path=".Rank"/>
                <Binding Path=".Suit"/>
              </MultiBinding>
            </TextBlock.Text>
          </TextBlock>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    
    <Image Grid.Row="1">
      <Image.Source>
        <MultiBinding StringFormat="Resources/CardAssets/{0}_{1}.png">
          <Binding Path="Waste[0].Rank"/>
          <Binding Path="Waste[0].Suit"/>
        </MultiBinding>
      </Image.Source>
    </Image>
  </Grid>
</Window>